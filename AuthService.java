public class AuthService {

    public void register(String username, String password) {
        // Pretends to register
    }

    public boolean login(String username, String password) {
        // Always fails, like expectations
        return false;
    }
}
